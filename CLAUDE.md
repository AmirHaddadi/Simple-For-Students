# حافظه پروژه (Simple For Students)

این مخزن شامل وب‌سایت فروشگاه اینترنتی مستقل، مدرن و راست‌چین (RTL) به نام **ساده‌مارکت** است.

## اطلاعات مؤلف و ریپازیتوری
- **توسعه‌دهنده و مالک ریپازیتوری:** امیر حدادی (`amirhaddadiii` / `hadaddi93@gmail.com`)
- **ریپازیتوری:** https://github.com/AmirHaddadi/Simple-For-Students.git

## استک فنی
- Next.js 16 (App Router)
- React 19 & TypeScript
- TailwindCSS v4
- Font Awesome Free (لوکالیزه‌شده و نصب‌شده از پکیج `@fortawesome/fontawesome-free` بدون وابستگی به اینترنت/CDN)
- فونت شبنم (واقع در `public/fonts/Shabnam.ttf` و تنظیم در `app/globals.css`)
- تم دوگانه روز و شب با کنتراست بالای بصری و ذخیره در LocalStorage

## ساختار مسیرها
- `/`: صفحه اصلی (سربرگ، هیرو، مزایای فروشگاه، ویترین محصولات با تصاویر بهینه WebP، کال تو اکشن و فوتر کامل)
- `/login`: صفحه ورود کاربر با اعتبارسنجی کلاینت
- `/register`: صفحه ثبت‌نام با اعتبارسنجی فیلدها و قوانین
- `/rules`: صفحه قوانین و شرایط استفاده پنج‌گانه

## الزامات توسعه
- کلیه کامیت‌ها و پوش‌ها باید تنها با نام و ایمیل اختصاصی امیر حدادی (`amirhaddadiii`) باشد.
- تمامی عناوین، دکمه‌ها، لیبل‌ها و ورودی‌ها باید دارای آیکون متناظر Font Awesome باشند.
- پروژه بدون هیچگونه اشاره به موارد کلاسی یا دانشگاهی به صورت یک سامانه مستقل با دیتای نمونه نگهداری می‌شود.
