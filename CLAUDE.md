# حافظه پروژه (Simple For Students)

این مخزن شامل وب‌سایت فروشگاه اینترنتی مستقل، مدرن و راست‌چین (RTL) به نام **ساده‌مارکت** است.

## اطلاعات مؤلف و ریپازیتوری
- **توسعه‌دهنده و مالک ریپازیتوری:** امیر حدادی (`amirhaddadiii` / `hadaddi93@gmail.com`)
- **ریپازیتوری:** https://github.com/AmirHaddadi/Simple-For-Students.git

## استک فنی و ویژگی‌ها
- **Next.js 16 (App Router)** & **React 19** & **TypeScript**
- **TailwindCSS v4**
- **Font Awesome Free (Local):** لوکالیزه‌شده و نصب‌شده از پکیج `@fortawesome/fontawesome-free` بدون وابستگی به اینترنت یا CDN. تمامی عناوین، لیبل‌ها، اینپوت‌ها و دکمه‌ها در کل پروژه دارای آیکون متناظر هستند.
- **فونت فارسی شبنم:** واقع در `public/fonts/Shabnam.ttf` و تنظیم در `app/globals.css`.
- **تم دوگانه روز و شب:** پالت تیره اختصاصی با پس‌زمینه تیره و عمیق، کنتراست بالای بصری و کانتینر شیشه‌ای بلر در هیرو، با ذخیره‌سازی وضعیت در LocalStorage.
- **تصاویر بهینه‌شده:** تصاویر WebP در `public/images/` شامل تصویر هیرو و ۶ محصول اصلی.
- **غیرفعال‌سازی نشانگر توسعه Next.js:** تنظیم `devIndicators: false` در `next.config.ts`.

## ساختار مسیرها
- `/`: صفحه اصلی (شامل سربرگ، هیرو شیشه‌ای، مزایای فروشگاه، ویترین محصولات با تصاویر WebP، کال‌تو‌اکشن و فوتر کامل بدون کپی‌رایت دانشگاهی)
- `/login`: صفحه ورود کاربر با اعتبارسنجی کلاینت
- `/register`: صفحه ثبت‌نام با اعتبارسنجی فیلدها و قوانین
- `/rules`: صفحه قوانین و مقررات پنج‌گانه رسمی

## الزامات توسعه و نسخه‌سپاری
- تمامی کامیت‌ها و پوش‌ها منحصراً با نام و ایمیل کاربری امیر حدادی (`amirhaddadiii` / `hadaddi93@gmail.com`) ثبت می‌شوند و نام ارائه‌دهنده یا هوش مصنوعی در آن قرار نمی‌گیرد.
- پروژه بدون هیچ‌گونه اشاره به پروژه‌های دانشگاهی یا کلاسی و به شکل یک وب‌سایت مستقل تجاری با داده‌های نمونه نگهداری می‌شود.
- پکیج زیپ نهایی و تمیز (بدون ردپای AI، بدون گیت، بدون اطلاعات هویتی و پوشه‌های سنگین توسعه) با نام `simple-market.zip` در ریشه قرار دارد.
